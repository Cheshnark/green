import React from "react";
import Alert from '@mui/material/Alert';

const RedAlert = () => {
    return (
        <div className="red-alert">
            <Alert severity="success">Subscrito correctamente</Alert>
        </div>
    )
};

export default RedAlert;